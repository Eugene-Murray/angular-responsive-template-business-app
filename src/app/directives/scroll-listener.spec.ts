import { ScrollListener } from './scroll-listener';

describe('ScrollListener', () => {
  it('should create an instance', () => {
    const directive = new ScrollListener();
    expect(directive).toBeTruthy();
  });
});
